// Deprecated: Marketing content moved to web-landing/ (Next.js at hexmind.ai).
import { useEffect } from 'react';

export default function Features() {
  useEffect(() => {
    window.location.replace('https://hexmind.ai/features');
  }, []);
  return null;
}
